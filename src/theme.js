import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ea8025",
      dark: "#ea8025",
      contrastText: "#fff",
    },
    secondary: {
      main: "#fff7e6",
      dark: "#fff7e6",
      contrastText: "#ea8025",
    },
  },
});

export default theme;
